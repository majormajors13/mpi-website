<section id="identity" class="identity" aria-labelledby="identity-title">
  <div class="identity__inner container">
    <div class="identity__heading">
      <p class="identity__label">MPI // Independent workshop</p>
      <h2 id="identity-title">A place for the work, not just one project.</h2>
    </div>

    <div class="identity__body">
      <p>
        Major Problem Industries is the home for the work I keep building:
        software, AI, experiments, and whatever else grows out of a good
        question.
      </p>
      <p>
        LUNA is the flagship project right now, but it sits inside a broader
        workshop of tools, ideas, and things still being shaped into something
        useful.
      </p>
    </div>
  </div>
</section>

<style>
  .identity__inner {
    position: relative;
    display: grid;
    gap: var(--space-6);
    padding-block: clamp(var(--space-8), 10vw, 8rem);
  }

  .identity__inner::after {
    position: absolute;
    inset: auto 0 12% auto;
    width: 9rem;
    height: 5rem;
    border-block-end: 1px solid rgb(115 71 216 / 5%);
    border-inline-end: 1px solid rgb(115 71 216 / 5%);
    content: "";
    pointer-events: none;
  }

  .identity__label {
    color: var(--color-accent-highlight);
    font-family: var(--font-technical);
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.09em;
    text-transform: uppercase;
  }

  h2 {
    max-width: 38rem;
    margin-block-start: var(--space-3);
    font-size: clamp(2rem, 5vw, 3.75rem);
    letter-spacing: -0.045em;
    line-height: 1.05;
  }

  .identity__body {
    position: relative;
    display: grid;
    gap: var(--space-5);
    align-content: start;
    padding-block-start: var(--space-5);
    border-block-start: 1px solid var(--color-border);
    color: var(--color-text-muted);
    font-size: clamp(1rem, 2vw, 1.125rem);
  }

  .identity__body::before {
    position: absolute;
    inset-block-start: -1px;
    inset-inline-start: 0;
    width: 5rem;
    height: 1px;
    background: var(--color-accent-secondary);
    content: "";
    pointer-events: none;
  }

  @media (min-width: 52rem) {
    .identity__inner {
      grid-template-columns: minmax(0, 1.15fr) minmax(18rem, 0.85fr);
      gap: clamp(var(--space-7), 8vw, 7rem);
    }
  }
</style>
