<section id="about" class="about" aria-labelledby="about-title">
  <div class="about__inner container">
    <div class="about__heading">
      <p class="about__label">Operation // One human</p>
      <h2 id="about-title">What Drives Us</h2>
      <p class="about__statement">
        Major Problem Industries is where curiosity turns into software,
        hardware, and whatever else the problem calls for.
      </p>
    </div>

    <div class="about__body">
      <p>
        Major Problem Industries is a one-man show built around one thing:
        forging your own path. I came into software and AI from years of
        hands-on trade work, and that background still shapes how I approach
        everything I build: understand the system, learn the hard parts, and
        make something that actually works.
      </p>
      <p>
        A big part of why I’m building all of this is my family. My wife and
        kids are a constant reminder to keep learning, keep building, and keep
        pushing for something better. I want my kids to grow up seeing that you
        can change direction, learn difficult things, and build your own path at
        any point in life.
      </p>
    </div>
  </div>
</section>

<style>
  .about {
    padding-block: clamp(var(--space-8), 10vw, 8rem);
  }

  .about__inner {
    display: grid;
    gap: var(--space-7);
  }

  .about__label {
    color: var(--color-accent-highlight);
    font-family: var(--font-technical);
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  h2 {
    margin-block-start: var(--space-3);
    font-size: clamp(2.75rem, 7vw, 5rem);
    letter-spacing: -0.055em;
    line-height: 1;
  }

  .about__statement {
    max-width: 38rem;
    margin-block-start: var(--space-5);
    font-size: clamp(1.25rem, 3vw, 1.75rem);
    font-weight: 600;
    line-height: 1.35;
  }

  .about__body {
    display: grid;
    max-width: var(--content-width-reading);
    gap: var(--space-5);
    align-content: start;
    padding-block-start: var(--space-5);
    border-block-start: 1px solid var(--color-border);
    color: var(--color-text-muted);
  }

  @media (min-width: 52rem) {
    .about__inner {
      grid-template-columns: minmax(0, 0.9fr) minmax(24rem, 1.1fr);
      gap: clamp(var(--space-7), 8vw, 7rem);
    }
  }
</style>
